
services:
  backend:
    build: ./backend
    container_name: Shell_Backend
    env_file:
      - ./backend/.env
    ports:
      - "3000:3000"
    restart: always

  frontend:
    build:
      context: ./frontend
      args:
        VITE_BASE_URL: ${VITE_BASE_URL}
        VITE_RAZORPAY_KEY: ${VITE_RAZORPAY_KEY}
    container_name: Shell_Frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always
